var abp = abp || {};

abp.modals.mailEventCreate = function () {
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("XICommServer");
        
        
        
        
        
    };

    return {
        initModal: initModal
    };
};
